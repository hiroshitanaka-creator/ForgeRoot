{
  "name": "@forgeroot/auditor",
  "version": "0.0.0-t041",
  "private": true,
  "type": "module",
  "description": "ForgeRoot auditor runtime, T040 SARIF bridge, and T041 deterministic security gate decision manifest.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "npm run build && node --test --test-force-exit tests/*.test.mjs"
  },
  "engines": {
    "node": ">=22.5"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
