# apps

Executable applications for ForgeRoot live here.

Planned application boundaries:

- `github-app/` — GitHub App control plane
- `cli/` — operator and bootstrap CLI
- `browser-extension/` — review and observability surface

T001 seeds boundaries only. Runtime implementation belongs to later tasks.
