# crates

Rust crates live here.

Planned early crates:

- `forge-kernel/` — deterministic `.forge` parser and canonical hash kernel
- `forge-replay/` — replay and recovery support

T001 fixes the boundary and naming, not the implementation.
