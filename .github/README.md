# .github

GitHub-facing metadata and automation live here.

`workflows/` is created in T001 only as a structural boundary.
Real workflow definitions are intentionally out of scope until later tasks.
