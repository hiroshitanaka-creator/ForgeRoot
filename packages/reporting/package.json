{
  "name": "@forgeroot/reporting",
  "version": "0.0.0-t068",
  "private": true,
  "type": "module",
  "description": "ForgeRoot deterministic reporting artifacts.",
  "scripts": {
    "prebuild": "node ../../scripts/clean-package-dist.mjs",
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=22.5"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
