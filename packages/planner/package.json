{
  "name": "@forgeroot/planner",
  "version": "0.0.0-t016",
  "private": true,
  "type": "module",
  "description": "ForgeRoot planner primitives: deterministic intake classification and one-task-one-PR Plan Spec DSL.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test --test-force-exit tests/*.test.mjs"
  },
  "engines": {
    "node": ">=22.5"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
