{
  "name": "@forgeroot/github-pr-adapter",
  "version": "0.0.0-t025",
  "private": true,
  "type": "module",
  "description": "ForgeRoot GitHub PR adapter: deterministic GitHub App pull request creation request preparation from PR composition manifests.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "npm run build && node --test --test-force-exit tests/*.test.mjs"
  },
  "engines": {
    "node": ">=22.5"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
