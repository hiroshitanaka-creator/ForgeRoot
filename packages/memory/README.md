# @forgeroot/memory

Phase 2 memory foundation package for deterministic, auditable manifest artifacts.

This package only creates and validates `working_memory_update` and `episode_digest` manifests. It does not write `.forge`, call GitHub APIs, implement MemoryKeeper, compute eval scores, build vector indexes, or treat runtime databases as source of truth.
