{
  "name": "@forgeroot/memory",
  "version": "0.0.0-t031",
  "private": true,
  "type": "module",
  "description": "ForgeRoot Phase 2 deterministic memory foundation manifests.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test --test-force-exit tests/*.test.mjs"
  },
  "engines": { "node": ">=22.5" },
  "devDependencies": { "typescript": "^5.8.3" }
}
