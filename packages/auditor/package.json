{
  "name": "@forgeroot/auditor",
  "version": "0.0.0-t040",
  "private": true,
  "type": "module",
  "description": "ForgeRoot auditor runtime and T040 deterministic SARIF-like finding bridge.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "npm run build && node --test --test-force-exit tests/*.test.mjs"
  },
  "engines": {
    "node": ">=22.5"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
