{
  "name": "@forgeroot/forge-demo",
  "version": "0.0.0-t070",
  "private": true,
  "type": "module",
  "description": "ForgeRoot demo harnesses for T028 forged PR flow and T070 distributed evolution manifests.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=22.5"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
