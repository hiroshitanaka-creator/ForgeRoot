{
  "name": "@forgeroot/network",
  "version": "0.0.0-t067",
  "private": true,
  "type": "module",
  "description": "ForgeRoot deterministic federation boundary manifests.",
  "scripts": {
    "prebuild": "node ../../scripts/clean-package-dist.mjs",
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=22.5"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
